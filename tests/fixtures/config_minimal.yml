version: 1
model:

  # User
  - name: User
    properties:
      - name: name
        type: String
        required: true

  # Project
  - name: Project
    properties:
      - name: name
        type: String
      - name: description
        type: String
      - name: status
        type: String
      - name: priority
        type: Int
      - name: estimate
        type: Float
      - name: active
        type: Boolean
    relationships:
      - name: owner
        properties:
          - name: since
            type: String
        nodes: [User]
      - name: board
        nodes: [KanbanBoard, ScrumBoard]
      - name: activity
        nodes: [Commit]
        list: true
      - name: issues
        nodes: [Feature, Bug]
        list: true
  
  # Feature
  - name: Feature
    properties:
      - name: name
        type: String
  
  # Bug
  - name: Bug
    properties:
      - name: name
        type: String
  
  # KanbanBoard
  - name: KanbanBoard
    properties:
      - name: name
        type: String
  
  # ScrumBoard
  - name: ScrumBoard
    properties:
      - name: name
        type: String
  
  # Commit
  - name: Commit
    properties:
      - name: hash
        type: String

endpoints: []
