hosts: [127.0.0.1]
username: stephen
password: password
port: 8182
serializer: { className: org.apache.tinkerpop.gremlin.driver.ser.GraphBinaryMessageSerializerV1, config: { serializeResultToString: true }}
connectionPool: {
  enableSsl: true,
  trustStore: conf/server.jks
}