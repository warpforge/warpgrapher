# Valid config
---
version: 2
model:
  - name: Feature
    properties:
      - name: name
        type: String

  - name: Bug
    properties:
      - name: name
        type: String

  - name: KanbanBoard
    properties:
      - name: name
        type: String

  - name: ScrumBoard
    properties:
      - name: name
        type: String

  - name: Commit
    properties:
      - name: hash
        type: String
      
