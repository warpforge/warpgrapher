# Valid config
---
version: 2
model:
  - name: User
    properties:
      - name: name
        type: String
endpoints:
  - name: UserCount
    class: Query
    input:
      list: false
      type: UserQueryInput
    output:
      list: false
      type: Int
  - name: UserCount
    class: Query
    input:
      list: false
      type: UserQueryInput
    output:
      list: false
      type: Int
