# Invalid config
---
version: 2
model:
  - name: User
    properties:
      - name: name
        required: true
        type: "String"
  - name: Project
    properties:
      - name: name
        type: String
      - name: description
        type: String
      - name: status
        type: String
      - name: priority
        type: Int 
      - name: estimate
        type: Float
      - name: active
        type: Boolean
      - name: points
        type: Int 
    relationships:
      - name: owner
        properties:
          - name: ID 
            type: Int 
        nodes: [User]
